# 本地工具元数据

## 忽略范围

根目录 `.gitignore` 必须忽略以下根目录：

- `.agent/`
- `.agents/`
- `.claude/`
- `.codex/`
- `.comet/`
- `.cursor/`

## 已跟踪文件

上述目录中已经被 Git 跟踪的文件必须从索引移除，使后续提交可从远程仓库删除这些路径。取消跟踪不得删除本地目录或文件。

未列出的根目录独立说明文件不属于本能力的忽略或取消跟踪范围。
